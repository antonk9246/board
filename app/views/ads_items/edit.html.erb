<h1>Editing article</h1>
 
<%= form_for(@ads_items) do |f| %>
 
  <% if @ads_items.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@ads_items.errors.count, "error") %> prohibited
        this ads_items from being saved:
      </h2>
      <ul>
        <% @ads_items.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
 
  <p>
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </p>
 
  <p>
    <%= f.label :text %><br>
    <%= f.text_area :text %>
  </p>
 
  <p>
    <%= f.submit %>
  </p>
 
<% end %>
 
<%= link_to 'Back', ads_items_path %>